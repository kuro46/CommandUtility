package com.github.kuro46.commandutility.handle

/**
 * A casting error used by [CommandManager]
 */
enum class CastError {

    /**
     * Used when cannot cast to player.
     */
    CANNOT_CAST_TO_PLAYER,

    /**
     * Used when cannot cast to player.
     */
    CANNOT_CAST_TO_CONSOLE
}
